import { FilerSearchPipe } from './filer-search.pipe';

describe('FilerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
